#include<bits/stdc++.h>
using namespace std;
int a,b,c;
int main(){
	cin>>a>>b>>c;
	double delta=b*b-4*a*c;
	if(delta>0)printf("%.2f %.2f",(-1.0*b-sqrt(delta))/(2*a),(-1.0*b+sqrt(delta))/(2*a));
	else if(delta==0)printf("%.2f",-1.0*b/(2*a));
	else printf("NO SOLUTION");
}
